server:
  port: 8070

eureka:
  instance:
    hostname: localhost
  client:
    fetchRegistery: false
    registerWithEureka: false
    serviceUrl:
     defaultZone: http://${euruka.instance.hostname}:${server.port}/eureka/
